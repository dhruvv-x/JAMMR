<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.JAMMR" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.JAMMR" parent="Base.Theme.JAMMR" />

    <!-- Single tappable emoji in the message long-press reaction picker -->
    <style name="EmojiPickerItem">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">24sp</item>
        <item name="android:padding">6dp</item>
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

    <!-- Rounded corners for photo/video thumbnails inside chat bubbles -->
    <style name="RoundedImageCorners">
        <item name="cornerSize">12dp</item>
    </style>
</resources>